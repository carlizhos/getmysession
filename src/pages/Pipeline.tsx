import Layout from '@/components/Layout';
import KanbanBoard from '@/components/patients/KanbanBoard';

const Pipeline = () => {
    return (
        <Layout>
            <KanbanBoard />
        </Layout>
    );
};

export default Pipeline;
